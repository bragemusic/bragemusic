export enum PlayContextType {
  Album = "album",
  LikedTracks = "liked_tracks",
  Playlist = "playlist",
  TopTracks = "top_tracks",
  Filter = "filter",
}

export enum TrackSource {
  Context = "context",
  Queue = "queue",
}
